import Ember from 'ember';
import layout from '../../templates/components/emoji-picker/search';
import { PropTypes } from 'ember-prop-types';

export default Ember.Component.extend({
  layout,
  classNameBindings: [':emoji-mart-search'],

  propTypes: {

  }
});
